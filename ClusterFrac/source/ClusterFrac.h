#pragma once
#include "Task.h"
#include "Result.h"


//sf::Packet& operator <<(sf::Packet& packet, const std::vector<int>& character)
//{
//	return packet << character.data();
//}
//
//sf::Packet& operator >>(sf::Packet& packet, std::vector<int>& character)
//{
//	return packet >> character.data();
//}